"""MPC kernel version.

Bumped to 0.4.0 at Session 5a to mark the first formal pack layer built on
top of the RFC-001 kernel. This bundle's mpc_kernel/ is a shim-import
wrapper (see RFC-002 §6 / SESSION-5a-part2 §D2); the canonical per-file
split lives on the workstation.
"""

__version__ = "0.4.0"
